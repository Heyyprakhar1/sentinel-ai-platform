apiVersion: v1
kind: Service

metadata:
  namespace: sentinelai-dev
  name: sentinelai-frontend-service

spec:
  selector:
    app: sentinelai-frontend

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP

  type: ClusterIP
